[project]
name = 'whistledao'
description = ''
authors = []
telemetry = true
cache_dir = './.cache'
requirements = []
[contracts.verification-rewards]
path = 'contracts/verification-rewards.clar'
clarity_version = 3
epoch = 'latest'

[contracts.whistleblower-dao]
path = 'contracts/whistleblower-dao.clar'
clarity_version = 3
epoch = '3.2'
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
